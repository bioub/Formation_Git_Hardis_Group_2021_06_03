{
  "devDependencies": {
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.0"
  },
  "scripts": {
    "prepare": "husky install",
    "format": "prettier --write \"**/*.{json,md}\""
  },
  "lint-staged": {
    "*.{json,md}": "prettier --write"
  }
}
